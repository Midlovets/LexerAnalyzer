fun hello(name: String) {
    print("Hello, " + name)
}

fun myFun(a: Int, b: Int): Int = a + b

fun `fun`(): Int {
    val name: String = "Lero4ka"
    hello(name)
    val a = 5 + 10
    print(a)
    return a + 2
}

fun main() {
    val x: Int = -8; var y: Real = 3.0
    val s: String = "Hello"
    val name1: String = "Valeriia"
    var flag: Boolean = true

    val result = s + name1

    hello(name1)

    if (x > 0 && flag || !false) {
        print("positive")
    } else {
        print("negative")
    }

    when (x) {
        1 -> { print("one") }
        2 -> print("two or three")
        4 -> print("four")
        5 -> { val age: Int = 20 }
        else -> print("other")
    }

    for (i in 1..10) {
        print(i + ",")
    }
    print("")

    for (j in 5 downTo 1 step 1) {
        print(j + "; ")
    }
    print("")

    var k: Int = 0
    do {
        k = k + 1
    } while (k < 3)

    `fun`()

    val res: Int = myFun(2, 3)
    print("myFun = " + res)

    val message: String = if (res > 4) "Result is large" else "Result is small"
    print(message)

    print("Enter a number: ")
    val input: String = readLine()
    print("You entered: " + input)

    var a: Real = 2 + 3 - 1
    a = a * 4 / 2.0
    val exp: Real = 2.0 ** 3.0
    print("exp = " + exp)

    val quoted: String = "quoted"
    val backtick: Int = `fun`()
    print("backtick = " + backtick)
}