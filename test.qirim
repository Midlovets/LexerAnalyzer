fun main() {
    val x: Int = 5; var y: Real = 3
    val s: String = "Hello, "
    var flag: Boolean = true
    val name = "Valeriia"

    val result = "Hello, " + "Anfisa"
    print()

    if (x > 0 && flag || !false) {
        print("positive")
    } else {
        print("negative")
    }

    when (x) {
        1 -> { print("one") }
        2 -> print("two or three")
        4 -> print("four")
        5 -> print("five")
        else -> print("other")
    }

    for (i in 1..10) {
        print(i)
    }

    for (j in 5 downTo 1 step 1) {
        print(j)
    }

    var k: Int = 0
    do {
        k = k + 1
    } while (k < 3)
}